{
  "name": "masjidapp-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:web\" \"npm:dev:tv\" \"npm:dev:mobile\"",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:tv": "npm --prefix apps/tv run dev",
    "dev:mobile": "npm --prefix apps/mobile/web run dev",
    "build": "npm --prefix apps/web run build && npm --prefix apps/tv run build && npm --prefix apps/mobile/web run build",
    "lint": "echo 'Add linters as needed'"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "lucide-react": "^0.540.0"
  }
}
